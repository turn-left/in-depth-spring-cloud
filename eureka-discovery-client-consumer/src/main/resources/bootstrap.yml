# 配置中心相关
spring:
  application:
    # 对于配置文件{application}
    name: in-depth-spring-cloud
  cloud:
    config:
      # 对于配置文件所在分支，也是{label}部分
      label: master
      # 对于配置文件的{profile}
      profile: dev
      # 配置服务地址
      uri: http://127.0.0.1:8888/